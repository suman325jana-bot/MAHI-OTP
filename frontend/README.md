# Frontend (demo)

Install:
  npm install

Run:
  npm run dev

This is a tiny React + Vite demo that queries the backend /api/numbers/list endpoint.
Update VITE_API_BASE in .env to point to your backend if needed.
